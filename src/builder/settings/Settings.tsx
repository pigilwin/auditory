import { Title } from '../../components/Title';

export const Settings = (): JSX.Element => {
    return (
        <div className="mx-auto flex flex-wrap overflow-hidden min-h-screen">
            <div className="text-center w-full">
                <Title title="Settings"/>
            </div>
        </div>
    );
}